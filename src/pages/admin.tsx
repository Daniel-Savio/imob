import AddForm from "@/components/addForm";

export default function Admin() {
  return (
    <div className="h-screen bg-gradient-to-tl from-indigo-300 to-zinc-50 flex flex-col items-center">
      <AddForm></AddForm>
    </div>
  );
}
